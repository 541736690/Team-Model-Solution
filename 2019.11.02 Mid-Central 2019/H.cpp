#include <bits/stdc++.h>
using namespace std;

const int N = 10050, M = 14000050;

struct qry {
    int l, r, id;
};

vector<qry> qrs;
int n, m;
int ql, qr;
int cnt[M];
int num[N];
bool res[N];

void change(int x, int val) {
    x += cnt[];
}

void slide(int l, int r) {

}

int main() {
    cin >> n >> m;
    for(int i = 0; i < n; i++) {
        double a; cin >> a;
        num[i] = (int)(a * 1000000);
    }
    for(int i = 0; i < m; i++) {

    }
}